---
workflowName: discovery-workflow
creationDate: 2025-12-14
module: Interactive PDF Mapper
status: COMPLETE
stepsCompleted: [1, 2, 3, 4, 5, 6, 7, 8, 9]
---

# Workflow Creation Summary: Discovery Workflow

## Workflow Information

- **Name:** discovery-workflow
- **Module:** Interactive PDF Mapper
- **Created By:** TJ
- **Created:** 2025-12-14
- **Location:** `.bmad-user-memory/modules/interactive-pdf-mapper/workflows/discovery-workflow`

## Purpose

Autonomous PDF analysis and golden map creation using GLM4.5V vision detection with self-validation. Creates UI-friendly field coordinates for seamless React component generation.

## Generated Files

| File | Purpose |
|------|---------|
| `workflow.md` | Main workflow configuration and entry point |
| `steps/step-01-init.md` | Initialize workflow, validate inputs, check continuation |
| `steps/step-01b-continue.md` | Resume from checkpoint for large PDFs (100+ pages) |
| `steps/step-02-detect.md` | GLM4.5V field detection with page-by-page processing |
| `steps/step-03-validate.md` | Coordinate validation with auto-retry (max 7 attempts) |
| `steps/step-04-organize.md` | Hierarchical organization and CSS coordinate conversion |
| `steps/step-05-generate.md` | Generate Golden Map JSON, QA Report, Performance Metrics |
| `steps/step-06-cache.md` | Cache golden map in sidecar storage |
| `templates/golden-map-template.json` | Output schema template |
| `data/retry-parameters.json` | Retry configuration (7 attempts with parameter adjustment) |

## Quick Start Guide

### Running the Workflow

1. **Invoke the workflow:**
   ```
   Load and execute: .bmad-user-memory/modules/interactive-pdf-mapper/workflows/discovery-workflow/workflow.md
   ```

2. **Provide inputs when prompted:**
   - PDF file path
   - Sections-references JSON path (optional)
   - Configuration parameters (optional)

3. **Workflow runs autonomously:**
   - Detects all form fields using GLM4.5V
   - Validates coordinates (±0.5 pixel tolerance)
   - Organizes fields hierarchically
   - Generates golden map and reports
   - Caches results for future use

### Expected Outputs

1. **Golden Map JSON** - `golden-map-{project_name}.json`
   - All field coordinates (PDF and CSS)
   - Hierarchical organization (Section → Subsection → Entry → Field)
   - UI hints for React component generation

2. **QA Report** - `qa-report-{project_name}.md`
   - Validation results and accuracy metrics
   - Field type breakdown
   - Recommendations

3. **Performance Metrics** - `metrics-{project_name}.json`
   - Processing times
   - Accuracy scores
   - Cache recommendations

## Key Features

- **Autonomous Operation**: Minimal user intervention required
- **Continuation Support**: Resume processing for large PDFs (100+ pages)
- **Auto-Retry Logic**: Up to 7 attempts with adjusted parameters
- **Verbose Progress**: Detailed per-page status updates
- **Hierarchical Output**: Section → Subsection → Entry → Field structure
- **Dual Coordinates**: Both PDF and CSS coordinates included
- **Sidecar Caching**: 99.8% performance improvement on subsequent access

## Next Steps

1. **Test the Workflow**: Run with a sample PDF to verify functionality
2. **Create Remaining Workflows**:
   - `interactive-workflow` - User form editing
   - `evolution-workflow` - GLM4.5V improvement tracking
   - `common-workflows` - Shared utilities
3. **Integrate with React Generation Agent**: Use golden map output for UI generation
4. **Run Compliance Check**: Validate against BMAD standards in a fresh context

## Support

For issues or questions about this workflow, refer to:
- Module README: `.bmad-user-memory/modules/interactive-pdf-mapper/README.md`
- Module Plan: `.bmad-user-memory/modules/interactive-pdf-mapper/module-plan.md`
- Workflow Plan: `workflow-plan-discovery-workflow.md`

---

*Generated by BMAD Create-Workflow on 2025-12-14*
